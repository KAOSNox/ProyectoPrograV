@model ProyectoProgra5.Models.Cargo

@{
    ViewData["Title"] = "Editar Partido";
}

<div class="card">
    <div class="card-header">
        <h1 class="text-center">Formulario para editar Cargo</h1>
    </div>
    <hr />
    <div class="card-body">
        <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
            <!--Input Nombre-->
            <div class="mb-3">
                <label asp-for="Nombre" class="form-label">Nombre</label>
                <input asp-for="Nombre" class="form-control" value="@Model.Nombre">
            </div>
            <!--Input Descipcion-->
            <div class="mb-3">
                <label asp-for="Descripcion" class="form-label">Fecha Fundacion</label>
                <input asp-for="Descripcion" class="form-control" value="@Model.Descripcion">
            </div>
            <div class="mb-3 text-center">
                <button type="submit" class="btn btn-warning">Editar</button>
                <a href="/Cargos/" class="btn btn-danger">Cancelar</a>
            </div>
        </form>
        
    </div>
</div>